# Agent Checklist

このリポジトリでエージェントが作業する時の標準手順。

1. `tasks/active/*.md` を読む。
2. タスクに書かれた `Required Inputs` と `Working Directories` を確認する。
3. `context/` と既存資料を読む。
4. ネットワーク上の事例、関連論文、公式情報を調査する。
5. URL、PDF、要約を所定の evidence ディレクトリに分けて記録する。
6. 解決策や実現方法を 3 案ほど提示する。
7. ユーザ確認を受けてから編集する。
8. コードは関数単位、文章や論文は小段落単位で編集する。
9. `logs/YYYY-MM-DD.md` に作業内容、根拠、未解決点、次の依頼文を残す。
10. `status.md` を更新する。

禁止事項:

- 明示許可なしに既存ファイルを移動・リネーム・削除しない。
- 根拠のない市場規模、論文品質、出願締切を確定情報として書かない。
- 調査と要約を成果物本文に混ぜない。

